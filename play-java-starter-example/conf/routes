# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# home page
GET     /                           controllers.AppController.index
GET   /search                       controllers.AppController.search(query: String, category: String, request: Request)
